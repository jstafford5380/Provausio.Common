version: 2
jobs:
    build:
      docker:
        - image: docker:17.05.0-ce-git
      steps:
        - checkout  
        - setup_remote_docker
        - docker build -t build-only --build-arg build_number=${CIRCLE_BUILD_NUM} --build-arg nuget_key=${PROV_NUGET_KEY}